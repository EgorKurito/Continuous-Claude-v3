# Continuous Claude - Руководство на русском языке

> Полное руководство по использованию Continuous Claude на русском языке

---

## Содержание

1. [Как общаться с Claude](#как-общаться-с-claude)
2. [Русские команды](#русские-команды)
3. [Система активации навыков](#система-активации-навыков)
4. [Быстрая установка](#быстрая-установка)
5. [Диагностика и исправление](#диагностика-и-исправление)

---

## Как общаться с Claude

**Вам не нужно запоминать slash-команды.** Просто опишите, что вы хотите, естественным языком.

### Примеры на естественном языке

| Что вы говорите | Что активируется |
|--------------|----------------|
| "Исправь сломанный логин" | `/fix` workflow → debug-agent, scout |
| "Построй дашборд пользователя" | `/build` workflow → plan-agent, kraken |
| "Я хочу понять эту кодовую базу" | `/explore` + scout agent |
| "Что может пойти не так с этим планом?" | `/premortem` |
| "На сегодня всё" | `create_handoff` (критично) |
| "Продолжи с того места, где остановились" | `resume_handoff` |
| "Найди доки по React hooks" | `docs-lookup` + Context7 |

---

## Русские команды

### Slash-команды

| Команда | Альтернатива | Что делает |
|---------|-------------|-----------|
| `/исправить` | `/фикс` | Workflow исправления багов |
| `/построить` | `/билд` | Workflow разработки функций |
| `/исследовать` | `/изучить` | Исследование кодовой базы |
| `/найди-доки` | `/docs-lookup` | Поиск документации через Context7 |
| `/коммит` | `/commit` | Создание git коммита |
| `/отладка` | `/debug` | Отладка проблемы |

### Триггеры на естественном языке

Система понимает русские фразы:

- "исправь баг", "сломано", "не работает" → `/fix`
- "построй", "создай", "добавь функцию" → `/build`
- "что это за код", "как работает" → `/explore`
- "найди доки", "документация по" → `/docs-lookup`

---

## Система активации навыков

### Что видит Claude

Когда вы отправляете сообщение, система анализирует его и показывает Claude релевантные навыки:

```
> "Исправь баг с логином в auth.py"

🎯 SKILL ACTIVATION CHECK / ПРОВЕРКА АКТИВАЦИИ НАВЫКОВ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 RECOMMENDED SKILLS / РЕКОМЕНДУЕМЫЕ НАВЫКИ:
  → fix
  → debug

🤖 RECOMMENDED AGENTS / РЕКОМЕНДУЕМЫЕ АГЕНТЫ:
  → debug-agent
  → scout
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Уровни приоритета

| Уровень | Описание |
|---------|----------|
| ⚠️ **CRITICAL** | Обязательны к использованию |
| 📚 **RECOMMENDED** | Следует использовать |
| 💡 **SUGGESTED** | Рассмотрите использование |
| 📌 **OPTIONAL** | Было бы полезно |

### Предупреждения о контексте

| Контекст % | Уровень | Действие |
|-----------|---------|----------|
| 70-79% | Уведомление | "Подумайте о handoff" |
| 80-89% | Предупреждение | "Рекомендация: /create_handoff" |
| 90%+ | **КРИТИЧНО** | "Запустите /create_handoff СЕЙЧАС!" |

---

## Быстрая установка

### Предварительные требования

```bash
# Проверить установку
python3 --version  # ≥3.11
uv --version
docker --version
claude --version
```

### 3 команды для проверки

```bash
cd /path/to/Continuous-Claude-v3
bash quick-check.sh    # Проверка (1 мин)
bash quick-fix.sh      # Исправление (3 мин)
claude                 # Запуск
```

### Ручная установка

```bash
# 1. Клонировать
git clone https://github.com/EgorKurito/Continuous-Claude-v3.git
cd Continuous-Claude-v3

# 2. Запустить Docker
cd opc && docker compose up -d

# 3. Создать .env
cat > .env << 'EOF'
DATABASE_URL=postgresql://claude:claude_dev@localhost:5432/continuous_claude
CLAUDE_PROJECT_DIR=/path/to/Continuous-Claude-v3
CONTEXT7_API_KEY=your_key_here
EOF

# 4. Установить зависимости
uv sync --extra embeddings --extra postgres

# 5. Запустить wizard
uv run python -m scripts.setup.wizard
```

---

## Диагностика и исправление

### Скрипты диагностики

| Скрипт | Назначение |
|--------|-----------|
| `quick-check.sh` | Проверка всех компонентов |
| `quick-fix.sh` | Автоматическое исправление |

### Частые проблемы

**Docker контейнер не запущен:**
```bash
cd opc && docker compose up -d
docker ps --filter name=continuous-claude-postgres
```

**Таблицы не созданы:**
```bash
docker exec -i continuous-claude-postgres psql -U claude -d continuous_claude < docker/init-schema.sql
```

**Backend: local вместо postgres:**
```bash
# Проверить .env
cat opc/.env | grep DATABASE_URL

# Переустановить зависимости
cd opc && uv sync --extra embeddings --extra postgres
```

**Hooks не работают:**
```bash
cd ~/.claude/hooks && npm run build
```

### Контрольный чеклист

- [ ] Docker контейнер запущен (healthy)
- [ ] PostgreSQL: 4 таблицы созданы
- [ ] Файл `opc/.env` существует
- [ ] Зависимости установлены
- [ ] `~/.claude/` заполнен (agents, skills, hooks)
- [ ] Store/Recall learning работает

---

## Context7 - Документация библиотек

### Использование

```bash
# Через slash-команду
/docs-lookup react hooks
/найди-доки prisma

# Естественным языком
"Найди документацию по Next.js server actions"
"Покажи доки по tailwind"
```

### Поддерживаемые библиотеки

React, Next.js, Vue, Prisma, TypeScript, Tailwind, Express, FastAPI, Django, PyTorch и 40+ других.

### Автоматическое определение

При чтении файлов с импортами из популярных библиотек, Claude автоматически предложит использовать Context7 для получения актуальной документации.

---

## Skill vs Workflow vs Agent

| Тип | Назначение | Пример |
|-----|---------|---------|
| **Skill** | Инструмент для одной цели | `commit`, `tldr-code` |
| **Workflow** | Многошаговый процесс | `/fix`, `/build` |
| **Agent** | Специализированная подсессия | scout, oracle, kraken |

---

## Советы

1. **Будьте конкретны**: "Исправь баг с auth" лучше чем "что-то не так"
2. **Упомяните домен**: "Построй React компонент" активирует нужные навыки
3. **Доверяйте предложениям**: Если CRITICAL - следуйте указаниям
4. **Используйте естественный язык**: "Что может сломаться?" = `/premortem`

---

## Ссылки

- [README.md](../README.md) - Основная документация
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт (EN)
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура системы
